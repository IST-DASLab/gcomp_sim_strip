#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='gcomp_sim',
      version='0.1.0',
      description='Gradient Compression Simulation Framework',
      packages=find_packages(),
      )
